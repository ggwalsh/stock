# Stock

Drop a SKU export. Get min, max, and safety stock a counter can run.

Same source [geoffwalsh.xyz](https://geoffwalsh.xyz/tools/stock) runs. A template and a sample CSV sit in `public/`.

```bash
npm install
npm run dev
```

Opens on http://localhost:5173
